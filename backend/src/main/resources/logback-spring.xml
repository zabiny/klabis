<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Register custom converters -->
    <conversionRule conversionWord="threadOrRequest"
                    converterClass="com.klabis.common.logging.ThreadOrRequestConverter"/>
    <conversionRule conversionWord="highlightLevel" converterClass="com.klabis.common.logging.HighlightLevelConverter"/>

    <springProperty scope="context" name="appName" source="spring.application.name"/>

    <!-- Console appender with colors and new format -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %threadOrRequest [corr=%X{correlationId}] [user=%replace(%X{userId}){'^$', '-'}] %-5highlightLevel %cyan(%logger{36}) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender for production profile (no colors) -->
    <springProfile name="prod">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>logs/klabis-backend.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>logs/klabis-backend.%d{yyyy-MM-dd}.log</fileNamePattern>
                <maxHistory>30</maxHistory>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %threadOrRequest [corr=%X{correlationId}] [user=%replace(%X{userId}){'^$', '-'}] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
    </springProfile>

    <!-- Non-prod loggers configuration -->
    <springProfile name="!prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.klabis" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
    </springProfile>

    <!-- Prod loggers configuration -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
        <logger name="com.klabis" level="INFO" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </logger>
    </springProfile>

</configuration>
